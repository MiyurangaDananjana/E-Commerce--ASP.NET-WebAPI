@model IEnumerable<e_com_RSEt_API.Models.ComputerManufacturer>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ManufacturersName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ManufacturersName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ManufacturersId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ManufacturersId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ManufacturersId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
